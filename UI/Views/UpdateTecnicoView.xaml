<Window x:Class="UI.Views.UpdateTecnicoView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UI.Views"
        mc:Ignorable="d"
        Title="Editar técnico" Height="460" Width="420" Background="#F2F2F2" WindowStartupLocation="CenterScreen">
    <Grid HorizontalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Editar técnico" FontSize="18" FontWeight="DemiBold" VerticalAlignment="Center" Margin="0,20,0,0"/>
        <Grid Grid.Row="1" Margin="0,30,0,0" HorizontalAlignment="Center">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="200"/>
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="35"/>
                <RowDefinition Height="35"/>
                <RowDefinition Height="35"/>
                <RowDefinition Height="35"/>
                <RowDefinition Height="35"/>
                <RowDefinition Height="35"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Id:" Grid.Row="0" Grid.Column="0" FontSize="14" FontWeight="DemiBold" VerticalAlignment="Center"/>
            <TextBlock Text="{Binding Id, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Grid.Row="0" Grid.Column="1" FontSize="14" FontWeight="DemiBold" VerticalAlignment="Center" Padding="2,0,0,0"/>
            <TextBlock Text="Nombre:" Grid.Row="1" Grid.Column="0" FontSize="14" FontWeight="DemiBold" VerticalAlignment="Center"/>
            <TextBox Text="{Binding Nombre, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Grid.Row="1" Grid.Column="1" Width="200" Height="30" VerticalAlignment="Center" VerticalContentAlignment="Center"/>
            <TextBlock Text="Apellidos:" Grid.Row="2" Grid.Column="0" FontSize="14" FontWeight="DemiBold" VerticalAlignment="Center"/>
            <TextBox Text="{Binding Apellidos, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Grid.Row="2" Grid.Column="1" Width="200" Height="30" VerticalAlignment="Center" VerticalContentAlignment="Center"/>
            <TextBlock Text="Gaveta:" Grid.Row="3" Grid.Column="0" FontSize="14" FontWeight="DemiBold" VerticalAlignment="Center"/>
            <TextBox Text="{Binding Gaveta, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, TargetNullValue=''}" Grid.Row="3" Grid.Column="1" Width="200" Height="30" VerticalAlignment="Center" VerticalContentAlignment="Center"/>
            <TextBlock Text="NombrePC:" Grid.Row="4" Grid.Column="0" FontSize="14" FontWeight="DemiBold" VerticalAlignment="Center"/>
            <TextBox Text="{Binding NombrePC, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Grid.Row="4" Grid.Column="1" Width="200" Height="30" VerticalAlignment="Center" VerticalContentAlignment="Center"/>
            <TextBlock Text="UsuarioPC:" Grid.Row="5" Grid.Column="0" FontSize="14" FontWeight="DemiBold" VerticalAlignment="Center"/>
            <TextBox Text="{Binding UsuarioPC, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Grid.Row="5" Grid.Column="1" Width="200" Height="30" VerticalAlignment="Center" VerticalContentAlignment="Center"/>

        </Grid>

        <TextBlock Grid.Row="2" Text="{Binding Message}" Foreground="{Binding MessageColor}" FontSize="14" Margin="0,30"/>
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button
                Grid.Column="0"
                Content="Cancelar"
                Command="{Binding CancelEditarTecnicoCommand}"
                Width="120"
                Padding="8"
                Margin="10,0,0,0"
                HorizontalAlignment="Left"
                Background="#ECECEC"
                FontSize="14"
                FontWeight="DemiBold"/>
            <Button
                Grid.Column="1"
                Content="Guardar"
                Command="{Binding SaveEditarTecnicoCommand}"
                Width="120"
                Padding="8"
                Margin="0,0,10,0"
                HorizontalAlignment="Right"
                Background="#ECECEC"
                FontSize="14"
                FontWeight="DemiBold"/>
        </Grid>
    </Grid>
</Window>
